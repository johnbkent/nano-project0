<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Button Style for the non-Capstone buttons. Based on Chimdindu Aneke's post in the forums. -->
    <style  name="ButtonStyle">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">#F08C35</item>
        <item name="android:textColor">#000000</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_marginTop">25dp</item>
        <item name="android:padding">@dimen/abc_text_size_button_material</item>
    </style>

    <style name="CapstoneButtonStyle" parent="ButtonStyle">
        <item name="android:background">#AD1100</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

</resources>
